<Window
    x:Class="VRA.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="clr-namespace:VRA.App.Views"
    xmlns:controls="clr-namespace:VRA.App.Controls"
    mc:Ignorable="d"
    Title="VRA Professional"
    Width="1600"
    Height="900"
    WindowStartupLocation="CenterScreen"
    Background="{DynamicResource BackgroundBrush}">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="32"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="#30363D"
                BorderThickness="0,0,0,1">

            <Grid Margin="20,0">

                <Grid.ColumnDefinitions>

                    <ColumnDefinition Width="250"/>

                    <ColumnDefinition Width="*"/>

                    <ColumnDefinition Width="Auto"/>

                </Grid.ColumnDefinitions>

                <!-- Logo -->

                <StackPanel Orientation="Horizontal"
                            VerticalAlignment="Center">

                    <TextBlock Text="🧸"
                               FontSize="28"/>

                    <TextBlock Margin="12,0,0,0"
                               FontSize="24"
                               FontWeight="Bold"
                               Text="VRA Professional"/>

                </StackPanel>

                <!-- Patient -->

                <ComboBox Grid.Column="1"
                          Width="320"
                          Height="38"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center">

                    <ComboBoxItem>No patient selected</ComboBoxItem>

                </ComboBox>

                <!-- Status -->

                <StackPanel Grid.Column="2"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">

                    <Ellipse Width="12"
                             Height="12"
                             Fill="LimeGreen"/>

                    <TextBlock Margin="8,0,20,0"
                               VerticalAlignment="Center"
                               Text="Ready"/>

                    <Button Width="38"
                            Height="38"
                            Content="⚙"/>

                </StackPanel>

            </Grid>

        </Border>

        <!-- Content -->

        <Grid Grid.Row="1">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <controls:SidebarControl
    Grid.Column="0"
    DataContext="{Binding Sidebar}" />

            <!-- Current Page -->
            <ContentControl
        Grid.Column="1"
        Content="{Binding CurrentView}"/>

        </Grid>
        <Grid Grid.Row="1">
            <ContentControl Content="{Binding CurrentView}" />
        </Grid>

        <!-- Footer -->

        <StatusBar Grid.Row="2">

            <StatusBarItem Content="Ready"/>

        </StatusBar>

    </Grid>

</Window>